services:
  - type: web
    name: nostalgia-console-server
    runtime: node
    rootDir: mini-services/retro-console-server
    buildCommand: bun install --frozen-lockfile
    startCommand: bun index.ts
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
